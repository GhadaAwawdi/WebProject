CREATE TABLE ebookUser
    (id  INT PRIMARY KEY NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) ,
    username VARCHAR(10) NOT NULL,
    email VARCHAR(20) NOT NULL,
    address VARCHAR(20) NOT NULL,
    telephoneNumber VARCHAR(10) NOT NULL,
    password VARCHAR(8) NOT NULL,
    nickname VARCHAR(20) NOT NULL,
    shortDescription VARCHAR(50),
    photo VARCHAR(45)
    );

CREATE TABLE ebook
	(id  INT PRIMARY KEY NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) ,
  	name VARCHAR(20) NOT NULL,
  	image VARCHAR(20),
  	price INT NOT NULL,
    shortDescription VARCHAR(50),
	likesNum INT NOT NULL,
	review VARCHAR(50) NOT NULL
 	);
 
CREATE TABLE likes
 	(userId  INT NOT NULL,
 	 bookId  INT NOT NULL ,
 	 nickname VARCHAR(20) NOT NULL,
 	 FOREIGN KEY (bookId) REFERENCES ebook(id)
 	);
 	
CREATE TABLE reviews
 	(bookId INT NOT NULL,
 	 userId INT NOT NULL,
 	 nickname VARCHAR(20) NOT NULL,
 	 review VARCHAR(50) NOT NULL,
 	 FOREIGN KEY (bookId) REFERENCES ebook(id)
 	);

CREATE TABLE purchase
 	(userId  INT NOT NULL,
 	 bookId  INT NOT NULL,
 	 FOREIGN KEY (userId) REFERENCES ebookUser(id)
 	);
 	

CREATE TABLE manager
	(id INT PRIMARY KEY NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1),
	 username VARCHAR(10) NOT NULL,
     password VARCHAR(8) NOT NULL
	);
	
--	INSERT INTO MANAGER (username,password) VALUES ('admin','Passw0rd');
 	
--DROP TABLE REVIEWS;
--DROP TABLE LIKES;
--	DROP TABLE ebookUser;
--	DROP TABLE ebooks;
--	DROP TABLE PIC;
--DROP TABLE purchase;
--	DROP TABLE test;
	
--	ALTER TABLE reviews
--	ADD FOREIGN KEY BOOKID REFERENCES test(bookId);
	
	Select * from MANAGER;
	